#include <iostream>

using namespace std;

int t;
string a, b;

int main()
{
    cin >> t;
    for(int i = 0; i < t; i++)
    {
        cin >> a >> b;
        if(a.length() >= b.length())
            int wynik[a.length()];
        else
            int wynik[b.length()];
    }

    return 0;
}
